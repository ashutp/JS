console.log("NUll is used to demonstrate intensionally absance of value\n");

// when you declare a variable but do not initialize it, it gets the value undefined.
// this happens automatically, so you do not have full control.
// but when you set a variable to null, you do it on purpose, which means you clearly say "this has no value".

let a=null;
console.log(a);
